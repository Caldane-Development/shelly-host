# Production overlay.
#
# Pulls prebuilt images from the local registry (registry:2 on the prod host)
# instead of building from source, so the production host only needs the compose
# files + a .env (not the full repo).
#
# Images are pushed by CI to 10.10.10.28:5000 and pulled here via localhost:5000
# (same registry, resolved locally on the host).
#
# Usage on the production host:
#   IMAGE_TAG=<git-sha> \
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --remove-orphans
#
# IMAGE_TAG defaults to "latest" when unset. The deploy workflow sets it to the
# short commit SHA so each release is pinned to an immutable image.
services:
  server:
    image: localhost:5000/shelly-host-server:${IMAGE_TAG:-latest}
    pull_policy: always

  client:
    image: localhost:5000/shelly-host-client:${IMAGE_TAG:-latest}
    pull_policy: always
    # The runtime (nginx) image doesn't need a TTY; leaving it on can keep an
    # SSH-driven `compose up -d` session open during deploy.
    tty: false

  # nginx "front door": the single published web entry point in production.
  # Publishes host port 4501 (the same port the OpenResty reverse proxy already
  # targets, so no OpenResty change is needed) and routes /api/* to the backend
  # server while serving the SPA from the client container.
  proxy:
    image: localhost:5000/shelly-host-proxy:${IMAGE_TAG:-latest}
    pull_policy: always
    container_name: shelly-proxy
    restart: unless-stopped
    depends_on:
      server:
        condition: service_healthy
      client:
        condition: service_started
    ports:
      - "4501:80"

